#pragma once

#include "imgui.h"

namespace cotm32::colors {
static constexpr inline auto OFF = IM_COL32(40, 40, 40, 255);
static constexpr inline auto RED = IM_COL32(255, 40, 40, 255);
static constexpr inline auto YELLOW = IM_COL32(255, 255, 40, 255);
static constexpr inline auto GREEN = IM_COL32(40, 255, 40, 255);
}  // namespace cotm32::colors
